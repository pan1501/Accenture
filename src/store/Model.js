/* Data logic / manipulation / cals */

export const Model = {
    // initial forms data gets stored here
    data: {},

    // get some data..
    getData: function () {
        return this.data;
    }
};
